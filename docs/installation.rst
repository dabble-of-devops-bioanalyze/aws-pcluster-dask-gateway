.. highlight:: shell

============
Installation
============


Stable release
--------------

To install aws_pcluster_dask_gateway, run this command in your terminal:

.. code-block:: console

    $ pip install aws_pcluster_dask_gateway

This is the preferred method to install aws_pcluster_dask_gateway, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for aws_pcluster_dask_gateway can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/bioanalyze/aws_pcluster_dask_gateway

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/bioanalyze/aws_pcluster_dask_gateway/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/bioanalyze/aws_pcluster_dask_gateway
.. _tarball: https://github.com/bioanalyze/aws_pcluster_dask_gateway/tarball/master
